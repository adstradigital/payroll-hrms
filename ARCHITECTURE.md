# Attendance Request System Architecture

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND (React)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  AttendanceRequests Component                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Table with requests                                   │   │
│  │ • Filter by status (All, Pending, Approved, Rejected)   │   │
│  │ • Search by employee name/ID                            │   │
│  │ • Approve/Reject buttons                                │   │
│  │ • Create new request modal                              │   │
│  │ • Bulk actions (select multiple)                        │   │
│  │ • Loading spinners                                      │   │
│  │ • Error alerts                                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│  attendanceApi Service Layer                                    │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • getRegularizationRequests()                            │   │
│  │ • createRegularizationRequest()                          │   │
│  │ • approveRequest()                                       │   │
│  │ • rejectRequest()                                        │   │
│  │ • updateRegularizationRequest()                          │   │
│  │ • deleteRegularizationRequest()                          │   │
│  │ • getPendingRequests()                                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│            HTTP Requests (with Bearer Token)                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↕
                    REST API Communication
                    (JSON Request/Response)
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (Django REST)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  AttendanceRegularizationRequestViewSet                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ ViewSet Actions:                                        │   │
│  │  ✓ list() - GET /regularization/                        │   │
│  │  ✓ create() - POST /regularization/                     │   │
│  │  ✓ retrieve() - GET /regularization/<id>/               │   │
│  │  ✓ update() - PUT /regularization/<id>/                 │   │
│  │  ✓ destroy() - DELETE /regularization/<id>/             │   │
│  │  ✓ @action pending() - GET .../pending/                 │   │
│  │  ✓ @action approve() - POST .../<id>/approve/           │   │
│  │  ✓ @action reject() - POST .../<id>/reject/             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│  AttendanceRegularizationRequestSerializer                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ • Validates data                                        │   │
│  │ • Read-only fields: employee_name, employee_id, etc.    │   │
│  │ • Serializes/Deserializes model data                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│  Database Layer                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ AttendanceRegularizationRequest Model                    │   │
│  │ ┌──────────────────────────────────────────────────┐    │   │
│  │ │ Fields:                                          │    │   │
│  │ │  • id (UUID)                                     │    │   │
│  │ │  • employee (FK to Employee)                     │    │   │
│  │ │  • attendance (FK to Attendance)                 │    │   │
│  │ │  • request_type (Choice)                         │    │   │
│  │ │  • requested_check_in (DateTime)                 │    │   │
│  │ │  • requested_check_out (DateTime)                │    │   │
│  │ │  • reason (Text)                                 │    │   │
│  │ │  • status (pending/approved/rejected/cancelled)  │    │   │
│  │ │  • reviewed_by (FK to Employee)                  │    │   │
│  │ │  • reviewed_at (DateTime)                        │    │   │
│  │ │  • reviewer_comments (Text)                      │    │   │
│  │ │  • created_at (DateTime)                         │    │   │
│  │ │  • updated_at (DateTime)                         │    │   │
│  │ └──────────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│  SQLite/PostgreSQL Database                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ attendance_attendanceregularizationrequest              │   │
│  │ (Table stores all regularization requests)             │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### Create Request Flow
```
┌─────────────┐
│ User fills  │
│ form in     │
│ modal       │
└──────┬──────┘
       │
       ↓
┌─────────────────────────────────┐
│ Component validates form data   │
│ (employee_id, date required)    │
└──────┬──────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────┐
│ POST /attendance/regularization/         │
│ {                                        │
│   employee: "uuid",                      │
│   attendance: "uuid",                    │
│   request_type: "missed_checkin",        │
│   requested_check_in: "2026-01-22T09:00" │
│   reason: "System was down"              │
│ }                                        │
└──────┬───────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────┐
│ Backend creates new request     │
│ Status: "pending"               │
└──────┬──────────────────────────┘
       │
       ↓
┌─────────────────────────────────┐
│ Return 201 Created with data    │
│ (includes id, created_at, etc)  │
└──────┬──────────────────────────┘
       │
       ↓
┌────────────────────────────┐
│ Frontend adds to requests   │
│ list and closes modal       │
└────────────────────────────┘
```

### Approve Request Flow
```
┌────────────────────────────┐
│ User clicks Approve button │
│ on pending request         │
└──────┬─────────────────────┘
       │
       ↓
┌────────────────────────────────────────┐
│ POST /attendance/regularization/{id}/  │
│        approve/                        │
│ { comments: "Approved" }               │
└──────┬───────────────────────────────────┘
       │
       ↓
┌───────────────────────────────┐
│ Backend:                      │
│ • Sets status = "approved"    │
│ • Sets reviewed_by = user     │
│ • Sets reviewed_at = now()    │
│ • Updates attendance times    │
└──────┬────────────────────────┘
       │
       ↓
┌───────────────────────────────┐
│ Return 200 OK with updated    │
│ request object                │
└──────┬────────────────────────┘
       │
       ↓
┌───────────────────────────────┐
│ Frontend:                     │
│ • Updates request status      │
│ • Hides action buttons        │
│ • Shows success/refresh UI    │
└───────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
Frontend                              Backend
   │                                    │
   │  1. User logs in                   │
   │────────────────────────────────────>
   │                                    │
   │  2. Returns access_token           │
   │<────────────────────────────────────
   │                                    │
   │  3. Store token in localStorage    │
   │                                    │
   │  4. API Request with Bearer token  │
   │  GET /attendance/regularization/   │
   │  Authorization: Bearer <token>     │
   │────────────────────────────────────>
   │                                    │
   │  5. Backend validates token        │
   │                                    │
   │  6. Return data (if authorized)    │
   │<────────────────────────────────────
   │                                    │
   │  7. If token expired (401)         │
   │<────────────────────────────────────
   │                                    │
   │  8. Refresh token request          │
   │────────────────────────────────────>
   │                                    │
   │  9. New access token               │
   │<────────────────────────────────────
   │                                    │
   │ 10. Retry original request         │
   │────────────────────────────────────>
```

---

## 🗄️ Database Schema

```
attendance_attendanceregularizationrequest

┌────────────────────────────────────┐
│ id (UUID) PRIMARY KEY              │
├────────────────────────────────────┤
│ employee_id (UUID) FK → Employee   │
├────────────────────────────────────┤
│ attendance_id (UUID) FK → Attendance│
├────────────────────────────────────┤
│ request_type (CharField)           │
│  • missed_checkin                  │
│  • missed_checkout                 │
│  • late_arrival                    │
│  • early_departure                 │
│  • full_day                        │
├────────────────────────────────────┤
│ requested_check_in (DateTime)      │
├────────────────────────────────────┤
│ requested_check_out (DateTime)     │
├────────────────────────────────────┤
│ reason (TextField)                 │
├────────────────────────────────────┤
│ status (CharField)                 │
│  • pending                         │
│  • approved                        │
│  • rejected                        │
│  • cancelled                       │
├────────────────────────────────────┤
│ reviewed_by_id (UUID) FK → Employee│
├────────────────────────────────────┤
│ reviewed_at (DateTime)             │
├────────────────────────────────────┤
│ reviewer_comments (TextField)      │
├────────────────────────────────────┤
│ created_at (DateTime) AUTO         │
├────────────────────────────────────┤
│ updated_at (DateTime) AUTO         │
└────────────────────────────────────┘

Relationships:
┌──────────────────┐
│  Employee        │
└──────┬───────────┘
       │
       │ (employee_id)
       │
┌──────▼────────────────────────────────────┐
│ AttendanceRegularizationRequest           │
└────────────────────────┬───────────────────┘
                         │
                         │ (reviewed_by_id)
                         │
                    ┌────▼──────────┐
                    │   Employee    │
                    │  (reviewer)   │
                    └───────────────┘
                    
┌──────────────────┐
│  Attendance      │
└──────┬───────────┘
       │
       │ (attendance_id)
       │
┌──────▼────────────────────────────────────┐
│ AttendanceRegularizationRequest           │
└────────────────────────────────────────────┘
```

---

## 🔄 State Management

### Frontend Component State
```javascript
State Variables:
├── requests: AttendanceRegularizationRequest[]
├── loading: boolean
├── error: string
├── activeTab: 'all' | 'pending' | 'approved' | 'rejected'
├── search: string
├── selected: UUID[]
├── showModal: boolean
└── submitting: boolean

State Transitions:
1. Initial Mount
   └─> loading = true
   └─> fetch requests
   └─> loading = false, requests = [...]

2. Filter by Status
   └─> activeTab = 'pending'
   └─> filteredRequests = filter()

3. Approve Request
   └─> submitting = true
   └─> POST approve
   └─> submitting = false
   └─> update local request status
```

---

## 📈 Request Flow Summary

```
┌─────────────────────────────┐
│  Frontend Component Mounts   │
└──────────────┬──────────────┘
               │
               ↓
        ┌──────────────┐
        │ Call API to  │
        │ GET requests │
        └──────┬───────┘
               │
               ↓
        ┌──────────────────────┐
        │ Populate requests[]   │
        │ in component state    │
        └──────┬───────────────┘
               │
               ↓
        ┌──────────────────┐
        │ Render table     │
        │ with requests    │
        └──────┬───────────┘
               │
               ↓
        ┌──────────────────┐
        │ User interacts:  │
        │ • Search         │
        │ • Filter         │
        │ • Select         │
        │ • Action         │
        └──────┬───────────┘
               │
    ┌──────────┼──────────────┐
    │          │              │
    ↓          ↓              ↓
  CREATE    APPROVE      REJECT
   │          │              │
   ↓          ↓              ↓
 POST        POST            POST
 /reg/    /reg/{id}/approve/ /reg/{id}/reject/
   │          │              │
   └──────────┼──────────────┘
              │
              ↓
        ┌──────────────────┐
        │ Update local     │
        │ requests state   │
        │ with response    │
        └──────┬───────────┘
               │
               ↓
        ┌──────────────────┐
        │ UI re-renders    │
        │ with new data    │
        └──────────────────┘
```

---

## 📱 Component Hierarchy

```
App
└── AttendanceRequests Component
    ├── Error Alert (conditional)
    ├── Header
    │   ├── Title
    │   └── Search Box + New Request Button
    ├── Toolbar
    │   ├── Status Tabs
    │   └── Bulk Actions (conditional)
    ├── Main Content
    │   ├── Loading Spinner (conditional)
    │   ├── Empty State (conditional)
    │   └── Table
    │       ├── Header Row
    │       └── Data Rows (map)
    │           ├── Checkbox
    │           ├── Employee Info
    │           ├── Request Type
    │           ├── Date
    │           ├── Status Badge
    │           └── Action Buttons
    └── CreateModal (conditional)
        ├── Header + Close Button
        ├── Form
        │   ├── Employee ID Input
        │   ├── Request Type Select
        │   ├── Date Input
        │   ├── Time Inputs
        │   ├── Reason Textarea
        │   └── Buttons (Cancel/Submit)
        └── Backdrop Click Listener
```

---

**Architecture Version**: 1.0.0
**Created**: January 22, 2026
**Status**: Complete & Implemented
